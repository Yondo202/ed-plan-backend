{
  "routes": [
    {
      "method": "GET",
      "path": "/pimis-edplan/marketings",
      "handler": "marketing.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/pimis-edplan/marketings/count",
      "handler": "marketing.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/pimis-edplan/marketings/:id",
      "handler": "marketing.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/pimis-edplan/marketings",
      "handler": "marketing.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/pimis-edplan/marketings/:id",
      "handler": "marketing.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/pimis-edplan/marketings/:id",
      "handler": "marketing.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
