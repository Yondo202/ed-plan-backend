{
  "routes": [
    {
      "method": "GET",
      "path": "/pimis-edplan/totals",
      "handler": "total.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/pimis-edplan/totals/count",
      "handler": "total.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/pimis-edplan/totals/:id",
      "handler": "total.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/pimis-edplan/totals",
      "handler": "total.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/pimis-edplan/totals/:id",
      "handler": "total.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/pimis-edplan/totals/:id",
      "handler": "total.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
