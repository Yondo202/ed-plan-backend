{
  "routes": [
    {
      "method": "GET",
      "path": "/pimis-edplan/firstpages",
      "handler": "firstpage.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/pimis-edplan/firstpages/count",
      "handler": "firstpage.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/pimis-edplan/firstpages/:id",
      "handler": "firstpage.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/pimis-edplan/firstpages",
      "handler": "firstpage.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/pimis-edplan/firstpages/:id",
      "handler": "firstpage.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/pimis-edplan/firstpages/:id",
      "handler": "firstpage.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
